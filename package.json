{
  "name": "expression-upload-notify",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "build": "node esbuild.js",
    "push": "clasp push",
    "open": "clasp open",
    "deploy": "npm run build && npm run push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "expression-upload-notify"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@google/clasp": "^3.1.1",
    "@types/google-apps-script": "^2.0.7",
    "esbuild": "^0.27.0",
    "esbuild-gas-plugin": "^0.10.0",
    "typescript": "^5.9.3"
  }
}
